# Copy outside the repository, replace the project ID, then apply in both namespaces.
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: google-secrets
spec:
  provider:
    gcpsm:
      projectID: REPLACE_WITH_YOUR_PROJECT_ID
