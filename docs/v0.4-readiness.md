# v0.4 and v0.5 acceptance status

Verified 19 September 2026. The required private-lab cloud checks passed under the [dated k3s exception](lab-security.md). The cumulative v0.5.0 metadata is prepared; the final source commit, its successful CI run and operator tags remain pending.

| Area | Verified evidence | Boundary |
|---|---|---|
| GitOps | Production snapshot from successful CI verified against exact source and image records; cloud Argo Healthy/Synced directly from Git without manual manifests; local failed migration blocks rollout and native configuration rollback restores the prior template | Automatic synchronization remains disabled; older-image rollback was not tested |
| Private ingress | Application and readiness return 200 through authenticated IAP with the private CA and correct TLS name; wrong hostname rejected; HTTPS blackbox probe succeeds | No public application endpoint |
| Secrets | Cloud workload federation allowed/denied identity tests; Secret Manager and ESO deliver rotated database and signing credentials; new database password works, old password fails, old signed session is rejected | Dedicated synthetic lab credentials; historical secret versions are retained |
| Telemetry | Web/database scrapes; request log matched to stored trace; synthetic sensitive marker absent from both; Prometheus alert reaches the lab receiver in firing and resolved states | The receiver records alerts locally; no external paging service |
| Scaling | Request-only HPA scales one to three replicas and back to one with CPU excluded; original HPA settings restored; local adapter-outage behavior already verified | Acceptance thresholds are test settings, not capacity recommendations |
| Recovery | Off-VM PostgreSQL archive restored into isolated storage; schema version, table counts, row checksums and synthetic canary matched; graceful node reboot retained the original database volume and historical logs, traces and metric samples | Retained-disk recovery and database restore were tested; full replacement-host recovery was not |
| Automation | Pinned platform charts, scoped federation and existing policies reconciled through Ansible; final full rerun reports 127 successful tasks, zero changes and zero failures with unchanged Helm revisions | Existing retained host; replacement-host recovery was not tested |
| Scheduled work | Both existing CronJob templates complete bounded catch-up runs after maintenance | Missed work is invoked deliberately |

Temporary restore resources, test canaries, alert rules and catch-up Jobs were removed after their checks. Backup evidence and operational material remain outside the repository. Database backups preserve sensitive-table schemas but exclude credential and cache payloads.

## Runtime gate

The exact pinned k3s image was rescanned on 19 September and still reports the same 26 fixable HIGH findings and no CRITICAL findings. The installed native binary matches the pinned checksum. The existing exception expires at 00:00 UTC on 20 September 2026 and has not been extended. Future deployment or configuration requires a valid reviewed gate; the general approval flag remains false. No scanner findings were suppressed.

Effective cloud firewall rules, the dedicated network, Shielded VM configuration and the host identity's absence of direct project grants were rechecked. All Kubernetes Services remain internal without NodePorts. Platform images use the verified import path; no registry credentials are present in the cluster.

The patched Argo image previously passed CI filesystem and derived-inventory scans and is running from its pinned imported digest. Prometheus Adapter exposes the request metric for opt-in request-based scaling; the normal CPU HPA settings were restored. KEDA is not required. Earlier Docker attestation verification used the published key with `--skip-tlog`; transparency verification is not claimed. These are dated lab acceptance results, not a vulnerability-free or public-service claim.
