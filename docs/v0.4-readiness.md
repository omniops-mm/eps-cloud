# v0.4 acceptance status

Verified 13 September 2026. Local acceptance and cloud smoke checks pass. Fresh-install corrections still require an operator commit, successful CI and a new production snapshot; the release is not accepted or tagged.

| Area | Evidence | Remaining boundary |
|---|---|---|
| Secrets and database | Local role/bootstrap checks; both cloud SecretStores and four ExternalSecrets Ready; four allowed/four denied cloud reads and an unapproved identity rejected | Cloud credential rotation and recovery |
| Telemetry | Real app/database scrapes, logs matched to traces, marker privacy, allowed/denied network paths, firing/resolved alerts | Cloud web/database scrapes pass; cloud log/trace correlation and broader sensitive-data review remain |
| Persistence | Pre-restart log/trace/historical metric and database canary survive pod replacement on unchanged PVCs | Node loss and retained off-site backups |
| Restore and rotation | Source archive restored into isolated PVC; new database password accepted/old rejected; signed state invalidated; both catch-up Jobs complete | Actual deployment maintenance and external backup retention |
| Ingress | Private CA verification, correct TLS name, internal Traefik readiness probe succeeds | Cloud app/readiness return 200 through IAP with CA/name verification; wrong TLS name rejected; HTTPS blackbox probe passes |
| Scaling | One native HPA; requests cause scale-up with CPU excluded; adapter outage returns error; recovery and downscale pass | Production capacity and thresholds |
| GitOps | Verified operator production snapshot; local failed migration blocks rollout and native configuration rollback restores the prior template; cloud manual sync succeeds with tested corrections | Publish corrected chart and resynchronize from Git; an older-image rollback remains unproven |

## Release blockers

- Argo CD 3.5.2 vendor image retains four fixable HIGH findings. The PCRE2/SQLite patch passed CI filesystem and complete derived-inventory scans and was published and signed in run 34765117259. Chart 10.9.0 passes local startup after Redis entrypoint and ApplicationSet configuration fixes. Cloud Argo and Redis run from imported digest-pinned images without registry credentials. Fresh deployment exposed HPA ordering and private-ingress health assumptions; corrected manual synchronization passed, but those chart changes are not yet published.
- k3s 1.36.4+k3s1 retains 26 fixable findings. The operator approved a [dated private-lab exception](lab-security.md); Terraform and Ansible require explicit selection. The dedicated cloud network, effective firewall rules, Shielded VM settings and IAP SSH are verified. The cloud application and telemetry passed the attended test. The bundled Traefik chart requires service.spec.type; the corrected value produces ClusterIP without NodePorts, and Ansible now verifies that boundary.

Prometheus Adapter 0.12.0 passed filesystem and verified signed-inventory checks and replaces the unselected KEDA route. Image review is time-specific; recheck exact digests before deployment. Docker attestation verification used its published key with `--skip-tlog`; transparency verification is not claimed. Finding counts are scanner occurrences, not proven exploit paths. No scanner ignore rules were added. The scoped k3s exception accepts residual risk; the Argo package candidate is a derived image, not a vendor-signed release.

Local workload/data credentials remain in Kubernetes, not source files. Probe tunnels, temporary Jobs/namespaces and canaries are cleaned up. Automatic production synchronization remains disabled; promotion is operator-controlled. Keep package/chart version metadata and release tags unchanged until the remaining acceptance gates pass.

The release-gate refresh on 13 September confirmed the current k3s scan still reports 26 findings. The Argo 3.5.2 DHI digest is unchanged; an upstream 3.5.2 scan reports 113 findings and the maintained DHI 3.4.8 alternative reports four. These alternatives do not clear the deployment gate. CI repository-write permissions and the bot promotion implementation have been removed; the operator verifies and commits production snapshots.

The cloud VM and local cluster were stopped after acceptance. The session SSH key was revoked and removed, and the IAP tunnel closed. Retained disks, secrets and Terraform state remain billable.
