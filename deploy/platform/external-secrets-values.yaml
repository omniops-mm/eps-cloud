# INSTALL BLOCKED: v2.10.0 has fixable HIGH CVE-2026-84304 and CVE-2026-84445.
# Select and rescan a patched image before installing this controller.
# Combine with a namespace-specific values file.
installCRDs: false
createOperator: false
scopedRBAC: true
replicaCount: 1
processClusterStore: false
processClusterExternalSecret: false
processPushSecret: false
processClusterPushSecret: false
resources:
  requests: {cpu: 25m, memory: 64Mi}
  limits: {cpu: 250m, memory: 192Mi}
securityContext:
  allowPrivilegeEscalation: false
  capabilities: {drop: [ALL]}
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  seccompProfile: {type: RuntimeDefault}
webhook:
  create: false
  certManager:
    enabled: true
    cert:
      issuerRef:
        group: cert-manager.io
        kind: Issuer
        name: eso-webhook
      privateKey:
        rotationPolicy: Always
  resources:
    requests: {cpu: 10m, memory: 32Mi}
    limits: {cpu: 100m, memory: 96Mi}
certController:
  create: false
  resources:
    requests: {cpu: 10m, memory: 32Mi}
    limits: {cpu: 100m, memory: 96Mi}

processClusterGenerator: false
rbac:
  serviceAccountTokenCreate: false
  # Grant ESO access explicitly; do not extend Kubernetes' default user roles.
  aggregateToView: false
  aggregateToEdit: false
  aggregateToAdmin: false
  servicebindings:
    create: false
