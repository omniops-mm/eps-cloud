# Copy to .env and fill in. The real .env is gitignored and never committed.

# Postgres connection string: postgresql+psycopg://user:password@host:port/dbname
DATABASE_URL=

# Flask session signing key. Generate one with:
#   uv run python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Local Postgres password, used by compose for the db and the app URL
POSTGRES_PASSWORD=

# Password for the read-only monitoring role postgres_exporter reads stats with.
# Letters, digits and dashes only; it is interpolated into an SQL literal.
POSTGRES_EXPORTER_PASSWORD=

# IANA timezone. Sets the container's own clock, and is the fallback the app
# uses for user-facing dates until one is chosen on the settings page.
TZ=Europe/Berlin